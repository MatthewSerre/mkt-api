<%= button_to 'Refresh API Key', refresh_api_key_path, method: :put, data: { confirm: "You are generating a new API key. The old one will no longer be valid and the new one will need to be used when accessing the API moving forward. You will only see the new API key once. Are you sure you want to continue?" } %>